Before we start — heads up: `brand/voice.md` is already fully populated (not stub/`<to be defined>` content). It covers all 8 framework sections with what looks like real, considered Echo Theory Labs voice (Applied Agentics, three pillars, peer-to-CTO register, etc.).

So "from scratch" is a meaningful choice. Three options:

1. **Rewrite from scratch** — archive the current `voice.md` to `voice.archive.md` and walk through all 8 sections fresh, treating the founder alignment as a reset.
2. **Revise specific sections** — keep the bones, re-author only the sections that shifted in the founder conversation (e.g., maybe personality, pillars, or attributes changed but terminology didn't).
3. **Reconcile** — I show you the current doc section by section, you flag what's now wrong, and we rewrite only those.

`★ Insight ─────────────────────────────────────`
- The framework is sequential by design: personality → attributes → audience → pillars → tone → terminology → anti-patterns. Earlier sections constrain later ones, so a "personality changed" edit usually cascades.
- The current doc is unusually disciplined — it offloads the "never list" to `anti-patterns.md` and grammar to `style-guide.md`, keeping `voice.md` focused on identity. Worth preserving that separation regardless of which path you pick.
`─────────────────────────────────────────────────`

Which path — 1, 2, or 3? And if it's 2, which sections shifted with the founders?
